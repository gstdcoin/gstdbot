# ═══════════════════════════════════════════════════════
# GSTD Node OS — Docker Compose
#
# One command to run your sovereign AI node:
#   docker compose up -d
#
# Dashboard: http://localhost:8080
# ═══════════════════════════════════════════════════════

services:
  gstd-node:
    image: gstdcoin/node:latest
    build: .
    container_name: gstd-node
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - gstd-data:/data/gstdbot
    environment:
      - NODE_ENV=production
      - GSTD_API_PORT=8080
      - SWARM_ENABLED=true
      - GSTD_MEMORY=true
      # Optional: Set your node name
      # - NODE_NAME=my-gstd-node
      # Optional: Telegram bot token
      # - TELEGRAM_BOT_TOKEN=your_token
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  gstd-data:
    driver: local
